# 이 파일은 "키를 어떤 이름으로 적어야 하는지" 보여 주는 견본이다. (GitHub에 올라감)
# 사용법: 이 파일을 복사해 이름을 .env 로 바꾸고, = 뒤에 실제 키를 붙여 넣는다.
# .env 는 .gitignore 에 등록돼 있어 GitHub에 올라가지 않는다.

# NAVER API HUB에서 발급받은 검색어트렌드 API 인증 정보
NAVER_API_KEY_ID=
NAVER_API_KEY=
